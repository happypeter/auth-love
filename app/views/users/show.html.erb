<p>
<table border=0>
<tr><td valign=top>Name:</td><td><%= @user.name %></td></tr>
<tr><td valign=top>Email:</td><td><%= @user.email %></td></tr>
</table>
</p>
<%= link_to "update profile", edit_user_path(@user)%>

<h2>Submissions:</h2>

<% if @user.posts.any? %>  
  <% @posts=Post.where(:user_id => @user.id) %> 
  <%= render 'posts/posthead' %>
<% else %>
  <p> no submission </p>
<% end %>
  
<h2>Comments:</h2>

<% @comments=Comment.where(:user_id => @user.id) %> 
<% if @comments.any? %>  
  <% @comments.each do |comment| %>
  <div>
    <div id="comment_head">
      <span class="comhead">
        <%= comment.created_at %>
        <% @post=Post.where(:id => comment.post_id).first %>
        on: <%= link_to @post.title, post_path(@post)%>
      </span>
      <br \>
    </div>
    <div id="comment_body">
      <%= raw Maruku.new(comment.body.to_s).to_html%>
    </div>
      <br \>
  </div>
  <% end %>
<% else %>
  <p> no comment </p>
<% end %>

