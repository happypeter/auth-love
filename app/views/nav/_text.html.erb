if you submit this:

    ```c
    #include <stdio.h>

    int main(int argc, const char *argv[])
    {
        printf("hello everyone!\n");
        return 0;
    }
    ```

the output:

```c
#include <stdio.h>

int main(int argc, const char *argv[])
{
    printf("hello everyone!\n");
    return 0;
}
```


NOTICE:

if you want to have

    <hi>

in your text, markdown will think it as a html tag, so it can not be shown as output

try this instead:

    `<hi>`

